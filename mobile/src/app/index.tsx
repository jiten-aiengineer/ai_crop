// This file is intentionally minimal — the root _layout.tsx handles all rendering
// Expo Router requires an index.tsx but our auth flow is managed in _layout.tsx
export default function Index() {
  return null;
}
